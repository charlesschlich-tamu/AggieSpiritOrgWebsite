import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // You can customize your theme here
    palette: {
        primary: {
            main: '#500000', // Aggie Maroon
        },
        secondary: {
            main: '#FFFFFF', // Gold
        },
    },
});

export default theme;
